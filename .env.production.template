# Ettametta Production Environment Template
# Rename this file to .env.production and fill in the values

# --- App Settings ---
APP_NAME="Ettametta API"
ENV="production"
DEBUG="false"
# Generate a secure random key: openssl rand -hex 32
SECRET_KEY="replace_with_secure_random_key_in_production"
ALGORITHM="HS256"

# --- Infrastructure ---
# The public domain of your deployed application (e.g. https://api.Ettametta.com)
# This is used for OAuth callbacks.
PRODUCTION_DOMAIN="https://your-domain.com"
PORT=8000

# --- Database & Redis ---
# Ensure these match your production database credentials
POSTGRES_USER=Ettametta_user
POSTGRES_PASSWORD=replace_with_secure_db_password
POSTGRES_DB=Ettametta_prod
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# --- AI & Neural Engines ---
# Required for Content Analysis and Ranking
GROQ_API_KEY="gsk_..."
OPENAI_API_KEY="sk-..."

# Voice Generation (Optional - falls back to mock)
ELEVENLABS_API_KEY="xi_..."

# Stock Media (Optional)
PEXELS_API_KEY=""

# --- Social Media API Keys ---
# For public data fetching
YOUTUBE_API_KEY=""
TIKTOK_API_KEY=""

# --- OAuth Credentials (CRITICAL for Publishing) ---
# See docs/deployment_guide.md for generation instructions

# Google Cloud Console -> APIs & Services -> Credentials
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# TikTok Developer Portal -> Apps
TIKTOK_CLIENT_KEY=""
TIKTOK_CLIENT_SECRET=""

# --- AWS S3 (Video Storage) ---
# Used for storing processed videos. If empty, local storage is used (ephemeral in Docker).
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_REGION="us-east-1"
AWS_STORAGE_BUCKET_NAME="viral-forge-videos"

# --- Video Engine ---
# Path to a font file in the container. Default is usually safe for the provided Docker image.
FONT_PATH="/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf"
