---
phase: 10-pipeline-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/api/utils/models.py",
  "src/services/discovery/schemas.py",
  "src/api/config/settings.py",
  "alembic/versions/2026_05_29_add_analysis_persistence_to_content_candidates.py",
  ".env.example",
  "tests/discovery/test_analysis_persistence.py"
]
autonomous: true
requirements: ["DISC-03", "VIDEO-01"]
user_setup: []
must_haves:
  truths:
    - "ContentCandidateDB persists analysis_task_id, analysis_status, analysis_payload, analysis_persisted_at, viral_score_velocity, recommended_style"
    - "AnalysisReport Pydantic model exists at src/services/discovery/schemas.py and is importable"
    - "ENABLE_PERSISTED_ANALYSIS feature flag is wired into settings and defaults to false for safe rollout"
    - "Alembic migration adds the new columns without dropping any existing data"
    - "Unit tests verify AnalysisReport validation and the feature flag"
  artifacts:
    - path: "src/services/discovery/schemas.py"
      provides: "Stable AnalysisReport Pydantic contract"
      contains: "class AnalysisReport"
    - path: "src/api/utils/models.py"
      provides: "ContentCandidateDB with new analysis persistence fields"
      contains: "analysis_payload = Column(JSON"
    - path: "alembic/versions/2026_05_29_add_analysis_persistence_to_content_candidates.py"
      provides: "Migration that adds the new columns"
      contains: "op.add_column"
    - path: "src/api/config/settings.py"
      provides: "ENABLE_PERSISTED_ANALYSIS feature flag"
      contains: "ENABLE_PERSISTED_ANALYSIS: bool = False"
  key_links:
    - from: "src/services/discovery/schemas.py"
      to: "src/api/utils/models.py"
      via: "AnalysisReport is the contract stored in analysis_payload"
      pattern: "AnalysisReport"
    - from: "src/api/config/settings.py"
      to: "src/services/discovery/tasks.py"
      via: "ENABLE_PERSISTED_ANALYSIS gates persistence in future 10-02"
      pattern: "ENABLE_PERSISTED_ANALYSIS"
---

<objective>
Lay the foundation for fixing the broken Discovery → Analysis → Video pipeline by introducing a stable, persisted analysis contract. This plan ships the schema and the type contract; subsequent plans (10-02..10-06) will use them to rewrite the Celery task, the status endpoint, the video job dispatch, the frontend wiring, and the E2E test.
</objective>

<execution_context>
Foundation plan. Run validation locally, then atomic commit. No need to spawn sub-agents — this is a focused 5-file change.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/BACKLOG.md (item 999.1)
@src/api/utils/models.py
@src/services/discovery/models.py
@src/services/discovery/tasks.py
@src/services/discovery/analysis_service.py
@src/api/routes/discovery.py
@src/api/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AnalysisReport Pydantic contract</name>
  <files>src/services/discovery/schemas.py</files>
  <read_first>src/services/discovery/models.py — see existing Pydantic style (ViralPattern, ContentCandidate)</read_first>
  <action>Create new file src/services/discovery/schemas.py with:
- AnalysisStatus enum (PENDING, RUNNING, COMPLETED, FAILED)
- HookInsights (first_3_seconds: str, emotional_angle: str, scroll_stopper: bool)
- PacingInsights (bpm: int, cuts_per_minute: float, recommended_duration_s: int)
- StructureInsights (arc: list[str], act_breaks: list[int], retention_curve: list[float])
- StyleInsights (recommended_style: str, color_palette: list[str], music_genre: str | None, motion_graphics: list[str])
- SentimentInsights (overall: str, target_audience: str, emotional_triggers: list[str])
- AnalysisReport Pydantic BaseModel with: id (str, default=uuid4), candidate_id (str), source_uri (str), analyzed_at (datetime, default_factory=now), hook (HookInsights), pacing (PacingInsights), structure (StructureInsights), style (StyleInsights), sentiment (SentimentInsights), summary (str), viral_score (float, ge=0, le=100), confidence (float, ge=0, le=1), raw_model_output (dict | None = None). Include .to_db_payload() and .from_db_payload() classmethods that handle the snake_case → JSONB roundtrip cleanly. Include a __str__ that returns a one-line summary for logs.</action>
  <verify>
    <automated>cd /home/psalmprax/ALL_PROJECTS/ettametta && python -c "from src.services.discovery.schemas import AnalysisReport, AnalysisStatus, HookInsights, PacingInsights, StructureInsights, StyleInsights, SentimentInsights; r = AnalysisReport(candidate_id='c1', source_uri='https://x', hook=HookInsights(first_3_seconds='Question', emotional_angle='curiosity', scroll_stopper=True), pacing=PacingInsights(bpm=120, cuts_per_minute=8, recommended_duration_s=30), structure=StructureInsights(arc=['setup','payoff'], act_breaks=[5], retention_curve=[1.0,0.8,0.6]), style=StyleInsights(recommended_style='educational', color_palette=['#fff'], music_genre='lofi', motion_graphics=['zoom']), sentiment=SentimentInsights(overall='positive', target_audience='creators', emotional_triggers=['curiosity']), summary='Test', viral_score=72, confidence=0.9); print(r.to_db_payload()['hook']['first_3_seconds'])"</automated>
  </verify>
  <done>File exists, AnalysisReport can be instantiated and roundtrips through to_db_payload/from_db_payload</done>
</task>

<task type="auto">
  <name>Task 2: Add analysis persistence columns to ContentCandidateDB</name>
  <files>src/api/utils/models.py</files>
  <read_first>src/api/utils/models.py — find ContentCandidateDB and note existing analysis_results/analyzed_at fields (keep them for backward compat)</read_first>
  <action>Add 6 new columns to ContentCandidateDB after analyzed_at:
- analysis_task_id: Column(String(64), nullable=True, index=True) — Celery task ID for polling
- analysis_status: Column(String(16), nullable=True) — one of PENDING|RUNNING|COMPLETED|FAILED
- analysis_payload: Column(JSON, nullable=True) — normalized AnalysisReport.to_db_payload()
- analysis_persisted_at: Column(DateTime, nullable=True) — when we wrote it to DB
- viral_score_velocity: Column(Float, nullable=True) — denormalized hot field for list rendering
- recommended_style: Column(String(64), nullable=True) — denormalized hot field for list rendering
All nullable=True so the existing rows continue to work. Add a docstring note explaining the contract: `analysis_payload` follows the AnalysisReport schema in src/services/discovery/schemas.py.</action>
  <verify>
    <automated>cd /home/psalmprax/ALL_PROJECTS/ettametta && python -c "from src.api.utils.models import ContentCandidateDB; cols = [c.name for c in ContentCandidateDB.__table__.columns]; print('analysis_task_id' in cols, 'analysis_payload' in cols, 'analysis_status' in cols, 'analysis_persisted_at' in cols, 'viral_score_velocity' in cols, 'recommended_style' in cols)"</automated>
  </verify>
  <done>ContentCandidateDB has 6 new columns; import succeeds; existing column list is preserved</done>
</task>

<task type="auto">
  <name>Task 3: Add ENABLE_PERSISTED_ANALYSIS feature flag</name>
  <files>src/api/config/settings.py, .env.example</files>
  <read_first>src/api/config/settings.py — find ENABLE_SOUND_DESIGN section (similar toggle pattern)</read_first>
  <action>Add `ENABLE_PERSISTED_ANALYSIS: bool = False` to the Settings class in src/api/config/settings.py — placed next to other analysis/discovery-related flags. Comment explains: "When True, analyze_viral_pattern_task persists results to ContentCandidateDB.analysis_payload instead of returning only via Celery backend. Safe rollout: default false in 10-01; flip to true in 10-02 once 10-02 plan is verified." Add a commented entry to .env.example: `# ENABLE_PERSISTED_ANALYSIS=false` in a "Discovery" section.</action>
  <verify>
    <automated>cd /home/psalmprax/ALL_PROJECTS/ettametta && python -c "from src.api.config.settings import settings; print('ENABLE_PERSISTED_ANALYSIS' in dir(settings), getattr(settings, 'ENABLE_PERSISTED_ANALYSIS', 'MISSING'))"</automated>
  </verify>
  <done>Settings.ENABLE_PERSISTED_ANALYSIS exists with default False; .env.example documents it</done>
</task>

<task type="auto">
  <name>Task 4: Generate Alembic migration for new columns</name>
  <files>alembic/versions/2026_05_29_add_analysis_persistence_to_content_candidates.py</files>
  <read_first>alembic/versions/ — see existing migration files for style</read_first>
  <action>Create a new Alembic migration file `alembic/versions/2026_05_29_add_analysis_persistence_to_content_candidates.py` with revision id `2026_05_29_analysis_persistence` and down_revision = None (this is a one-off, not a chained revision; alembic will auto-detect the head from the file system). Use op.add_column to add: analysis_task_id (String(64), nullable=True), analysis_status (String(16), nullable=True), analysis_payload (JSON, nullable=True), analysis_persisted_at (DateTime, nullable=True), viral_score_velocity (Float, nullable=True), recommended_style (String(64), nullable=True). Create an index on analysis_task_id. Add a downgrade() that drops the columns and index in reverse order. Wrap the columns in a single op.batch_alter_table block to be SQLite-safe (the project supports both SQLite and Postgres). Include a docstring explaining the purpose.</action>
  <verify>
    <automated>cd /home/psalmprax/ALL_PROJECTS/ettametta && python -c "import importlib.util; spec = importlib.util.spec_from_file_location('m', 'alembic/versions/2026_05_29_add_analysis_persistence_to_content_candidates.py'); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print('rev:', m.revision, 'down:', m.down_revision); print('has upgrade:', hasattr(m, 'upgrade'), 'has downgrade:', hasattr(m, 'downgrade'))"</automated>
  </verify>
  <done>Migration file imports cleanly; revision + down_revision are set; upgrade and downgrade both exist</done>
</task>

<task type="auto">
  <name>Task 5: Add unit tests for AnalysisReport + flag</name>
  <files>tests/discovery/test_analysis_persistence.py</files>
  <read_first>src/services/discovery/schemas.py — the contract being tested</read_first>
  <action>Create tests/discovery/test_analysis_persistence.py (mkdir -p tests/discovery first) with 4 tests:
1. test_analysis_report_roundtrip — build an AnalysisReport, dump via to_db_payload, rehydrate via from_db_payload, assert equality on all fields
2. test_analysis_report_validation_rejects_bad_viral_score — viral_score=150 should raise ValidationError
3. test_analysis_status_enum_values — assert all 4 enum values exist
4. test_persisted_analysis_flag_default_off — assert settings.ENABLE_PERSISTED_ANALYSIS is False by default
Use plain pytest (no async needed for these). Use the conftest from tests/ if it exists.</action>
  <verify>
    <automated>cd /home/psalmprax/ALL_PROJECTS/ettametta && python -m pytest tests/discovery/test_analysis_persistence.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discovery API → DB | AnalysisReport comes from LLM (untrusted text), must be validated before persisting |
| DB → Discovery API | Reads return normalized payload, not raw LLM text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|----------------|
| T-10-01-01 | Tampering | analysis_payload | mitigate | AnalysisReport Pydantic validates ranges, types, and required fields on every read AND write |
| T-10-01-02 | Information Disclosure | analysis_payload | accept | Payload is per-user; no PII expected. Will be reviewed in 10-06 |
| T-10-01-03 | DoS | JSONB column | mitigate | `analysis_payload` is bounded by AnalysisReport size (~10KB max). Future 10-06 plan will add length check on insert |
| T-10-01-04 | Repudiation | analysis_persisted_at | mitigate | DB column is auto-set on write; immutable once persisted |

</threat_model>

<verification>
1. `python -c "from src.services.discovery.schemas import AnalysisReport; ..."` — schemas importable
2. `python -c "from src.api.utils.models import ContentCandidateDB; ..."` — model has 6 new columns
3. `python -c "from src.api.config.settings import settings; print(settings.ENABLE_PERSISTED_ANALYSIS)"` — flag is False
4. `python -m pytest tests/discovery/test_analysis_persistence.py -v` — all tests pass
5. `alembic check` — migration is valid (if alembic is installed; otherwise, import-test the file)
</verification>

<success_criteria>
- 6 new columns on ContentCandidateDB
- AnalysisReport Pydantic model with full nested contract
- ENABLE_PERSISTED_ANALYSIS flag (default off)
- Alembic migration (additive, no data loss)
- 4 passing unit tests
- All existing tests still pass
</success_criteria>

<output>
After completion, create .planning/phases/10-pipeline-fixes/10-01-SUMMARY.md
</output>
