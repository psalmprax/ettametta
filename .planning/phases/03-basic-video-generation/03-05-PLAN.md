---
phase: 03-basic-video-generation
plan: 05
type: execute
wave: 5
depends_on: [03-04]
files_modified: [services/video_engine/tasks.py, services/storage/service.py, api/routes/video_generate.py]
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - Generated videos can be uploaded to cloud storage for preview
    - Video URLs are properly generated and accessible
    - Storage upload integrates seamlessly with video generation workflow
    - Users can access their generated videos via secure URLs
  artifacts:
    - path: services/storage/service.py
      provides: Enhanced storage service for video files
      exports: upload_file, get_public_url
    - path: api/routes/video_generate.py
      provides: Video preview endpoints
      exports: GET /video/{job_id}/preview
  key_links:
    - from: services/video_engine/tasks.py
      to: services/storage/service.py
      via: base_storage_service.upload_file
      pattern: "storage_key = base_storage_service.upload_file"
    - from: api/routes/video_generate.py
      to: services/storage/service.py
      via: get_public_url
      pattern: "base_storage_service.get_public_url"
---

<objective>
Implement storage upload and preview functionality for generated videos

Purpose: Enable users to access and preview their generated videos through secure cloud storage URLs
Output: Integrated storage upload in video generation pipeline and preview endpoints
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
@services/storage/service.py
@services/video_engine/tasks.py
@api/routes/video_generate.py
@03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate storage upload in video generation tasks</name>
  <files>services/video_engine/tasks.py</files>
  <action>Modify generate_video_task and generate_story_task to automatically upload completed videos to cloud storage using base_storage_service. Generate public URLs for video access and update job records with URLs.</action>
  <verify>Generated videos are automatically uploaded to storage and URLs are saved in job records</verify>
  <done>Storage upload integrated into video generation workflow</done>
</task>

<task type="auto">
  <name>Task 2: Add video preview endpoint</name>
  <files>api/routes/video_generate.py</files>
  <action>Create GET /video/{job_id}/preview endpoint that returns video metadata including public URL, status, and generation details. Include authentication and authorization checks.</action>
  <verify>GET /video/{job_id}/preview returns correct video information and URLs</verify>
  <done>Video preview endpoint implemented</done>
</task>

<task type="auto">
  <name>Task 3: Enhance storage service for video files</name>
  <files>services/storage/service.py</files>
  <action>Add video-specific handling to storage service including proper MIME types, video file validation, and optimized upload settings for large video files.</action>
  <verify>Video files upload correctly with proper metadata and are accessible via generated URLs</verify>
  <done>Storage service enhanced for video file handling</done>
</task>

<task type="auto">
  <name>Task 4: Add video job listing endpoint</name>
  <files>api/routes/video_generate.py</files>
  <action>Create GET /video/jobs endpoint that returns paginated list of user's video generation jobs with status, URLs, and metadata for easy preview access.</action>
  <verify>Users can list and access all their generated videos</verify>
  <done>Video job listing and management implemented</done>
</task>

</tasks>

<verification>
Generate a test video and verify it can be accessed via the preview URL
Test video job listing functionality
Verify storage URLs are secure and properly formatted
</verification>

<success_criteria>
- Generated videos are automatically uploaded to cloud storage
- Public URLs are generated and accessible for video preview
- Users can view and manage their generated videos through API endpoints
- Storage integration works reliably with different cloud providers
- Video files are properly validated and handled during upload
</success_criteria>

<output>
After completion, create .planning/phases/03-basic-video-generation/03-05-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/03-basic-video-generation/03-05-PLAN.md