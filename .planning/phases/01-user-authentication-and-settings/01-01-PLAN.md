---
phase: 01-user-authentication-and-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["api/utils/models.py", "api/routes/auth.py", "api/utils/auth.py", "alembic/versions/"]
autonomous: true
requirements: ["AUTH-01", "AUTH-02", "AUTH-03", "AUTH-04"]
user_setup: []
must_haves:
  truths:
    - "User can create an account with email and password"
    - "User can log in with email/password or Google OAuth and remain logged in across sessions"
    - "User can log out from any page"
  artifacts:
    - path: "api/utils/models.py"
      provides: "User data model"
      contains: "class UserDB"
    - path: "api/routes/auth.py"
      provides: "Authentication endpoints"
      exports: ["router"]
    - path: "api/utils/auth.py"
      provides: "Auth utilities"
      exports: ["create_access_token", "verify_token"]
  key_links:
    - from: "api/routes/auth.py"
      to: "api/utils/models.py"
      via: "UserDB import"
      pattern: "from .*models import UserDB"
    - from: "api/routes/auth.py"
      to: "api/utils/auth.py"
      via: "auth functions"
      pattern: "from .*auth import"
---

<objective>
Implement core user authentication system
Purpose: Enable secure user account creation, login, logout, and Google OAuth integration
Output: Functional auth API endpoints and database model
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@api/main.py
@api/utils/models.py
@api/routes/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define User database model</name>
  <files>api/utils/models.py</files>
  <action>Add UserDB SQLAlchemy model with fields: id (UUID), email (unique), password_hash, google_id (optional), created_at, updated_at. Ensure proper imports and base class inheritance.</action>
  <verify>
    <automated>pytest tests/test_models.py::test_user_model -x</automated>
  </verify>
  <done>UserDB model defined with all required fields and constraints</done>
</task>

<task type="auto">
  <name>Task 2: Create authentication utilities</name>
  <files>api/utils/auth.py</files>
  <action>Implement JWT token creation and verification functions using python-jose, password hashing with bcrypt, and Google OAuth client setup with authlib.</action>
  <verify>
    <automated>pytest tests/test_auth.py::test_create_token -x</automated>
  </verify>
  <done>Auth utilities implemented for JWT and OAuth</done>
</task>

<task type="auto">
  <name>Task 3: Implement auth API endpoints</name>
  <files>api/routes/auth.py</files>
  <action>Create FastAPI router with POST /auth/register (email/password validation), POST /auth/login (JWT token return), POST /auth/logout (token invalidation), GET /auth/google (OAuth redirect), GET /auth/google/callback (token creation).</action>
  <verify>
    <automated>pytest tests/test_auth.py::test_register_endpoint -x</automated>
  </verify>
  <done>All auth endpoints implemented and tested</done>
</task>

<task type="auto">
  <name>Task 4: Create database migration</name>
  <files>alembic/versions/001_create_user_table.py</files>
  <action>Generate Alembic migration to create user table with all fields from UserDB model.</action>
  <verify>
    <automated>alembic upgrade head && python -c "from api.utils.models import UserDB; print('Migration applied')"</automated>
  </verify>
  <done>User table created in database</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Untrusted user input for auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | Auth endpoints | mitigate | Validate email format, use secure JWT with expiration |
| T-01-02 | Tampering | Tokens | mitigate | Sign JWT with strong secret, validate on each request |
| T-01-03 | Repudiation | User actions | mitigate | Log auth events with user ID |
| T-01-04 | Information Disclosure | Passwords | mitigate | Hash passwords with bcrypt, never store plain text |
| T-01-05 | Denial of Service | Login attempts | mitigate | Rate limit auth endpoints |
| T-01-06 | Elevation of Privilege | OAuth callback | mitigate | Validate OAuth state parameter |
</threat_model>

<verification>
Run pytest on auth tests, verify endpoints return correct status codes
</verification>

<success_criteria>
User can register, login, logout, and use Google OAuth successfully
</success_criteria>

<output>
After completion, create .planning/phases/01-user-authentication-and-settings/01-01-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/01-user-authentication-and-settings/01-01-PLAN.md