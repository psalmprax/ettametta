---
phase: 01-user-authentication-and-settings
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["api/utils/models.py", "api/routes/settings.py", "api/utils/notifications.py"]
autonomous: true
requirements: ["AUTH-05", "SETTINGS-01"]
user_setup: []
must_haves:
  truths:
    - "User can configure Telegram and WhatsApp notifications via bots"
    - "User can manage system settings and API integrations"
  artifacts:
    - path: "api/utils/models.py"
      provides: "Updated User model with settings"
      contains: "telegram_chat_id, whatsapp_number"
    - path: "api/routes/settings.py"
      provides: "Settings management endpoints"
      exports: ["router"]
    - path: "api/utils/notifications.py"
      provides: "Notification utilities"
      exports: ["send_telegram_message", "send_whatsapp_message"]
  key_links:
    - from: "api/routes/settings.py"
      to: "api/utils/models.py"
      via: "UserDB update"
      pattern: "UserDB.*settings"
    - from: "api/routes/settings.py"
      to: "api/utils/notifications.py"
      via: "notification functions"
      pattern: "from .*notifications import"
---

<objective>
Implement user settings and notifications
Purpose: Allow users to configure notifications and manage API integrations
Output: Settings API endpoints and notification system
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@api/routes/auth.py
@api/utils/models.py
@api/utils/auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update User model for settings</name>
  <files>api/utils/models.py</files>
  <action>Add fields to UserDB: telegram_chat_id (optional), whatsapp_number (optional), api_keys (JSON), system_settings (JSON).</action>
  <verify>
    <automated>pytest tests/test_models.py::test_user_model -x</automated>
  </verify>
  <done>User model updated with settings fields</done>
</task>

<task type="auto">
  <name>Task 2: Create notification utilities</name>
  <files>api/utils/notifications.py</files>
  <action>Implement functions to send messages via Telegram Bot API and WhatsApp Business API using requests library.</action>
  <verify>
    <automated>pytest tests/test_notifications.py::test_send_telegram -x</automated>
  </verify>
  <done>Notification utilities implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement settings API endpoints</name>
  <files>api/routes/settings.py</files>
  <action>Create FastAPI router with GET /settings (retrieve user settings), PUT /settings (update settings including notifications and API keys).</action>
  <verify>
    <automated>pytest tests/test_settings.py::test_get_settings -x</automated>
  </verify>
  <done>Settings endpoints implemented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Untrusted input for settings updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | Settings data | mitigate | Validate input, store securely |
| T-01-08 | Information Disclosure | API keys | mitigate | Encrypt sensitive data in DB |
| T-01-09 | Spoofing | Bot integrations | mitigate | Verify bot tokens and user ownership |
</threat_model>

<verification>
Run pytest on settings tests, verify endpoints work
</verification>

<success_criteria>
User can update settings and receive notifications
</success_criteria>

<output>
After completion, create .planning/phases/01-user-authentication-and-settings/01-02-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/01-user-authentication-and-settings/01-02-PLAN.md