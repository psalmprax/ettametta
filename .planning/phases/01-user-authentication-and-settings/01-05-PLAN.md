---
phase: 01-user-authentication-and-settings
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - api/utils/models.py
  - api/routes/auth.py
autonomous: true
gap_closure: true
requirements: [AUTH-01]
must_haves:
  truths:
    - "UserDB model definitions are unified across files"
    - "Auth routes use the unified UserDB model"
  artifacts:
    - path: "api/utils/models.py"
      provides: "Unified UserDB model with Integer id and hashed_password"
    - path: "api/utils/user_models.py"
      provides: "Unified UserDB model"
  key_links:
    - from: "api/routes/auth.py"
      to: "api/utils/user_models.py"
      via: "import UserDB"
      pattern: "from.*user_models import UserDB"
---

<objective>
[Unify conflicting UserDB model definitions to resolve database column mismatch preventing user registration]

Purpose: [Fix the root cause of registration failure by standardizing UserDB schema across models.py and user_models.py]
Output: [Unified UserDB model with consistent field names and types, updated auth code]
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-user-authentication-and-settings/01-UAT.md
@.planning/debug/user-registration-failure.md
@.planning/phases/01-user-authentication-and-settings/01-01-SUMMARY.md
@api/utils/models.py
@api/utils/user_models.py
@api/routes/auth.py
@api/main.py
</context>

<tasks>

<task type="auto">
  <name>Unify UserDB model definitions and standardize field names</name>
  <files>api/utils/models.py,api/utils/user_models.py</files>
  <action>Merge UserDB definitions to use consistent Integer primary key, hashed_password field name, and include all necessary fields from both models. Update models.py UserDB to match user_models.py structure with Integer id and hashed_password, adding missing fields like google_id, api_keys, system_settings, and updated_at timestamp. Ensure both files define the same UserDB class for consistency.</action>
  <verify>
    <automated>grep -n "class UserDB" api/utils/models.py api/utils/user_models.py</automated>
  </verify>
  <done>UserDB models unified with Integer id and hashed_password field</done>
</task>

<task type="auto">
  <name>Update auth route to use unified UserDB model</name>
  <files>api/routes/auth.py</files>
  <action>Change import from api.utils.models to api.utils.user_models for UserDB. Update all references from password_hash to hashed_password. Change UserResponse id field from str to int to match Integer primary key. Update Google OAuth callback to set google_id field. Ensure registration and login work with unified schema.</action>
  <verify>
    <automated>pytest api/tests/test_auth.py -v</automated>
  </verify>
  <done>Auth endpoints use unified UserDB with correct field names</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted user input for registration/login |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | User registration | mitigate | Validate email format and password strength at API boundary |
| T-01-05-02 | Information Disclosure | User data | mitigate | Exclude password hash from API responses |
</threat_model>

<verification>
POST /auth/register returns 201 with user data excluding password
Database users table has correct columns matching code expectations
</verification>

<success_criteria>
User registration succeeds with POST /auth/register
No conflicting UserDB definitions
Auth code uses consistent field names
</success_criteria>

<output>
After completion, create .planning/phases/01-user-authentication-and-settings/01-05-SUMMARY.md
</output>