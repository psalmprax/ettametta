---
phase: 01-user-authentication-and-settings
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: ["api/routes/auth.py"]
autonomous: true
gap_closure: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "User can log out from any page"
    - "User can log in with email/password or Google OAuth and remain logged in across sessions"
  artifacts:
    - path: "api/routes/auth.py"
      provides: "Authentication endpoints with logout and OAuth fixes"
      contains: "logout endpoint with token invalidation, fixed OAuth callback"
  key_links:
    - from: "api/routes/auth.py"
      to: "Redis or blacklist mechanism"
      via: "token blacklist check"
      pattern: "blacklist.*token"
    - from: "api/routes/auth.py"
      to: "api/utils/models.py"
      via: "UserDB with role field"
      pattern: "user\\.role"
---

<objective>
Fix authentication gaps identified in verification: implement proper logout with token invalidation and fix Google OAuth callback field references.

Purpose: Complete the authentication system to fully support logout and OAuth login without bugs.
Output: Updated auth.py with working logout and OAuth callback.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-user-authentication-and-settings/01-01-SUMMARY.md
@.planning/phases/01-user-authentication-and-settings/01-02-SUMMARY.md
@.planning/phases/01-user-authentication-and-settings/01-VERIFICATION.md

# Key existing code
@api/routes/auth.py
@api/utils/models.py
@api/utils/auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement token blacklisting for logout</name>
  <files>api/routes/auth.py</files>
  <action>Implement JWT token blacklisting using Redis to invalidate tokens on logout. Add a blacklist set in Redis, add token to blacklist on logout, check blacklist in token verification. Use redis instance from app dependencies.</action>
  <verify>
    <automated>python -c "from api.routes.auth import router; from api.utils.auth import verify_token; print('Imports OK')"</automated>
  </verify>
  <done>Logout endpoint adds token to Redis blacklist, token verification checks blacklist</done>
</task>

<task type="auto">
  <name>Task 2: Fix Google OAuth callback field references</name>
  <files>api/routes/auth.py</files>
  <action>Add role field to UserDB model if needed, or remove role references. Change user.username to user.email in OAuth callback. Ensure callback uses correct UserDB fields: email instead of username.</action>
  <verify>
    <automated>python -c "from api.routes.auth import router; print('Callback imports OK')"</automated>
  </verify>
  <done>OAuth callback references valid UserDB fields (email, role handled)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted user input for auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | JWT tokens | mitigate | Implement token blacklisting to invalidate on logout |
</threat_model>

<verification>
Run automated tests for auth endpoints, verify imports work, check Redis integration for blacklist.
</verification>

<success_criteria>
Logout endpoint invalidates JWT tokens, OAuth callback works without field errors.
</success_criteria>

<output>
After completion, create .planning/phases/01-user-authentication-and-settings/01-03-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/01-user-authentication-and-settings/01-03-PLAN.md