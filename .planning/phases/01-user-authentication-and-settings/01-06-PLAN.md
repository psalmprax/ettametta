---
phase: 01-user-authentication-and-settings
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: ["api/utils/user_models.py", "api/utils/models.py"]
autonomous: true
gap_closure: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "User can create an account with email and password"
  artifacts:
    - path: "api/utils/user_models.py"
      provides: "Unified UserDB model"
      contains: "class UserDB"
    - path: "api/utils/models.py"
      provides: "Other models without UserDB"
      not_contains: "class UserDB"
  key_links:
    - from: "api/routes/auth.py"
      to: "api/utils/user_models.py"
      via: "import UserDB"
      pattern: "from api.utils.user_models import UserDB"
    - from: "api/main.py"
      to: "api/utils/user_models.py"
      via: "import UserDB"
      pattern: "from api.utils.user_models import UserDB"
---

<objective>
[Fix conflicting UserDB model definitions causing SQL column errors in user registration]

Purpose: [Resolve the root cause of registration failure by unifying UserDB schema and updating database]
Output: [Consistent UserDB model and database schema allowing successful user registration]
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-user-authentication-and-settings/01-UAT.md
@api/utils/models.py
@api/utils/user_models.py
@api/routes/auth.py
@api/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify UserDB in user_models.py</name>
  <files>api/utils/user_models.py</files>
  <action>Replace the UserDB class in user_models.py with the identical UserDB class from models.py, including UserRole and SubscriptionTier enums. Ensure created_at and updated_at use datetime.now(timezone.utc) for consistency.</action>
  <verify>
    <automated>grep -A 50 "class UserDB" api/utils/user_models.py | grep -c "hashed_password\|Integer\|timezone.utc" | grep -q "3" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>UserDB in user_models.py matches models.py exactly</done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate UserDB from models.py</name>
  <files>api/utils/models.py</files>
  <action>Remove the UserDB class, UserRole enum, and SubscriptionTier enum from models.py to eliminate duplication, as they are now centralized in user_models.py.</action>
  <verify>
    <automated>grep -c "class UserDB" api/utils/models.py | grep -q "0" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>UserDB removed from models.py</done>
</task>

<task type="auto">
  <name>Task 3: Recreate database tables with unified schema</name>
  <files></files>
  <action>Create and run a Python script to drop and recreate all database tables using the unified models from user_models.py and models.py. This ensures the database schema matches the code and resolves column mismatches.</action>
  <verify>
    <automated>python -c "
from api.utils.database import engine
from api.utils.user_models import Base as UserBase
from api.utils.models import Base as ModelBase
# Drop all tables
UserBase.metadata.drop_all(bind=engine)
ModelBase.metadata.drop_all(bind=engine)
# Recreate all tables
UserBase.metadata.create_all(bind=engine)
ModelBase.metadata.create_all(bind=engine)
print('Database tables recreated successfully')
"</automated>
  </verify>
  <done>Database schema updated to match unified UserDB model</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted user input for registration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Tampering | User registration | mitigate | Validate email format and password strength at API boundary |
</threat_model>

<verification>
[Confirm user registration works by testing POST /auth/register endpoint returns 201 with user data]
</verification>

<success_criteria>
[User registration test passes without SQL column errors]
</success_criteria>

<output>
After completion, create .planning/phases/01-user-authentication-and-settings/01-06-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/01-user-authentication-and-settings/01-06-PLAN.md