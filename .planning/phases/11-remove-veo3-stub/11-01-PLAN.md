---
phase: 11-remove-veo3-stub
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/services/video_engine/synthesis_service.py",
  "src/services/video_engine/engine_config.py",
  "src/services/payment/credit_service.py",
  "src/services/payment/stripe_service.py",
  "src/api/utils/subscription.py",
  "src/api/routes/content_editor.py",
  "src/api/routes/video_generate.py",
  "src/api/routes/video_jobs.py",
  "src/services/openclaw/skills/content.py",
  "src/services/video_engine/tests/test_generative_service.py",
  "src/api/tests/test_routes/test_video.py",
  "src/api/tests/test_api_comprehensive.py",
  "src/tests/test_video_generation.py",
  "src/tests/unit/test_video_job_service.py",
  "src/tests/e2e/tests/creation/video_creation.spec.ts",
  "src/tests/e2e/tests/creation/video_generation.spec.ts",
  "apps/dashboard/src/app/creation/page.tsx",
  "scratch/trigger_test_job.py"
]
autonomous: true
requirements: ["VIDEO-01"]
user_setup: []
must_haves:
  truths:
    - "grep -ri 'veo3' src/ apps/ scripts/ returns zero engine references"
    - "Default engine in route signatures and Pydantic schemas is 'ltx-video' (not 'veo3')"
    - "_synthesize_veo3 method is removed from synthesis_service.py"
    - "Engine selector in apps/dashboard no longer shows 'Google Veo 3'"
    - "User cannot trigger a video job with engine='veo3' (engine_config has no veo3 key)"
  artifacts:
    - path: "src/services/video_engine/engine_config.py"
      provides: "ENGINE_ACTION_MAP and PREMIUM_ENGINES without veo3"
      contains: "veo3" (count: 0)
    - path: "src/services/video_engine/synthesis_service.py"
      provides: "Video generation without _synthesize_veo3"
      contains: "_synthesize_veo3" (count: 0)
    - path: "apps/dashboard/src/app/creation/page.tsx"
      provides: "Engine selector without Veo3"
      contains: "veo3" (count: 0)
  key_links:
    - from: "src/api/routes/video_generate.py"
      to: "src/services/video_engine/engine_config.py"
      via: "default engine='ltx-video' resolves to a valid ENGINE_ACTION_MAP entry"
      pattern: "engine: str = .ltx-video."
---

<objective>
Stop users from being charged 25 credits for a fake Veo3 result. The current `_synthesize_veo3` path calls Gemini `generate_content` (which returns text, not video), logs the response, and falls through to Lite4K image+parallax. The fix is full removal: delete the method, drop it from all engine registries, replace defaults with `ltx-video`, and update tests + UI.
</objective>

<execution_context>
Single atomic commit. All edits are independent of each other. Run validation in parallel.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/BACKLOG.md (item 999.2)
@src/services/video_engine/synthesis_service.py
@src/services/video_engine/engine_config.py
@src/services/payment/credit_service.py
@src/services/payment/stripe_service.py
@src/api/utils/subscription.py
@src/api/routes/content_editor.py
@src/api/routes/video_generate.py
@src/api/routes/video_jobs.py
@src/services/openclaw/skills/content.py
@apps/dashboard/src/app/creation/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete _synthesize_veo3 + remove dispatch arms from synthesis_service.py</name>
  <files>src/services/video_engine/synthesis_service.py</files>
  <read_first>src/services/video_engine/synthesis_service.py lines 470-610, 825-940, 1060-1080</read_first>
  <action>Remove the following from src/services/video_engine/synthesis_service.py:
1. The entire `_synthesize_veo3` method (around lines 880-940)
2. The dispatch arm `if engine == "veo3": return await self._synthesize_veo3(prompt, aspect_ratio)` (around lines 597-598)
3. The default `engine: str = "veo3"` in `generate_video` (line 474) — change to `"ltx-video"`
4. The fallback `params = self._get_engine_params("veo3")` (line 504) — change to `"ltx-video"`
5. The default `engine: str = "veo3"` in `generate_scenes` (line 829) — change to `"ltx-video"`
6. The default `engine: str = "veo3"` in `apply_style_to_prompt` (line 1060) — change to `"ltx-video"`
7. Remove the `"veo3": "Google DeepMind aesthetics..."` entry from the style dict in `apply_style_to_prompt` (line 1072)
8. Remove the `"model": "veo3"` from `_synthesize_veo3` fallback path (line 916) — already covered by deleting the method</action>
  <verify>
    <automated>cd /home/psalmprax/ALL_PROJECTS/ettametta && grep -nE 'veo3|_synthesize_veo3' src/services/video_engine/synthesis_service.py | head -5</automated>
  </verify>
  <done>grep returns 0 lines for veo3 in synthesis_service.py</done>
</task>

<task type="auto">
  <name>Task 2: Remove veo3 from engine_config.py</name>
  <files>src/services/video_engine/engine_config.py</files>
  <read_first>src/services/video_engine/engine_config.py</read_first>
  <action>Remove `"veo3": "video_generation_veo3",` from `ENGINE_ACTION_MAP` and remove `"veo3",` from `PREMIUM_ENGINES` set.</action>
  <verify>
    <automated>cd /home/psalmprax/ALL_PROJECTS/ettametta && grep -c veo3 src/services/video_engine/engine_config.py</automated>
  </verify>
  <done>grep count for veo3 in engine_config.py is 0</done>
</task>

<task type="auto">
  <name>Task 3: Remove veo3 from credit_service.py + stripe_service.py</name>
  <files>src/services/payment/credit_service.py, src/services/payment/stripe_service.py</files>
  <read_first>src/services/payment/credit_service.py line 34; src/services/payment/stripe_service.py line 48</read_first>
  <action>Remove `"video_generation_veo3": 25,` from `CreditService.DEFAULT_COSTS` in credit_service.py. In stripe_service.py, change the STUDIO features list from `["Runway/Pika/Veo3/Wan2.2", ...]` to `["Runway/Pika/Wan2.2", ...]`.</action>
  <verify>
    <automated>cd /home/psalmprax/ALL_PROJECTS/ettametta && grep -c -i veo3 src/services/payment/credit_service.py src/services/payment/stripe_service.py</automated>
  </verify>
  <done>grep count for veo3 in both files is 0</done>
</task>

<task type="auto">
  <name>Task 4: Remove veo3 from subscription.py</name>
  <files>src/api/utils/subscription.py</files>
  <read_first>src/api/utils/subscription.py lines 118 and 228</read_first>
  <action>Remove `"veo3": SubscriptionTier.STUDIO,` from the engine_map dict (line 118) and remove `"veo3": {"daily_limit": 5, "premium": True, "provider": "Google"},` from quota_mapping (line 228).</action>
  <verify>
    <automated>cd /home/psalmprax/ALL_PROJECTS/ettametta && grep -c veo3 src/api/utils/subscription.py</automated>
  </verify>
  <done>grep count for veo3 in subscription.py is 0</done>
</task>

<task type="auto">
  <name>Task 5: Remove veo3 from content_editor.py + video_jobs.py + video_generate.py + openclaw content.py</name>
  <files>src/api/routes/content_editor.py, src/api/routes/video_jobs.py, src/api/routes/video_generate.py, src/services/openclaw/skills/content.py</files>
  <read_first>content_editor.py line 119; video_jobs.py line 140 and 235; video_generate.py lines 33,43,381,394; openclaw content.py lines 19,42</read_first>
  <action>content_editor.py: delete the entire `{"id": "veo3", "name": "Google Veo 3", ...}` dict entry. video_jobs.py: delete the `"veo3": {"credits": 25, ...}` entry; change default `engine=details.get("engine", "veo3")` to `"ltx-video"`. video_generate.py: change all 4 `engine: str = "veo3"` and `params.get("engine", "veo3")` to `"ltx-video"`. openclaw content.py: change both `engine: str = "veo3"` defaults to `"ltx-video"`.</action>
  <verify>
    <automated>cd /home/psalmprax/ALL_PROJECTS/ettametta && grep -c veo3 src/api/routes/content_editor.py src/api/routes/video_jobs.py src/api/routes/video_generate.py src/services/openclaw/skills/content.py</automated>
  </verify>
  <done>grep count for veo3 in all 4 files is 0</done>
</task>

<task type="auto">
  <name>Task 6: Remove veo3 from apps/dashboard + scratch + tests