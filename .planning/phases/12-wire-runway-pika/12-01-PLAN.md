---
phase: 12-wire-runway-pika
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/services/video_engine/ai_generator.py",
  "src/api/routes/engines.py",
  "src/api/main.py",
  ".env.example",
  "tests/video_engine/test_ai_generator_keys.py",
  "tests/api/test_engines_availability.py"
]
autonomous: true
requirements: ["VIDEO-01"]
user_setup: []
must_haves:
  truths:
    - "AIVideoGeneratorService._get_api_key() reads from settings.RUNWAY_API_KEY and settings.PIKA_API_KEY (not os.getenv)"
    - "With AI_VIDEO_PROVIDER=runway and RUNWAY_API_KEY set, the service is enabled and _get_api_key() returns a non-empty string"
    - "GET /engines/availability returns per-engine status for Runway, Pika, and the existing local engines (key_set, circuit_closed, enabled, provider)"
    - "RUNWAY_API_KEY and PIKA_API_KEY are documented in .env.example with usage notes"
    - "Unit tests verify settings-based key resolution and the /engines/availability response shape"
  artifacts:
    - path: "src/api/routes/engines.py"
      provides: "GET /engines/availability endpoint"
      contains: "router.get(\"/availability\")"
    - path: "src/services/video_engine/ai_generator.py"
      provides: "Settings-based key resolution"
      contains: "from src.api.config import settings"
    - path: ".env.example"
      provides: "RUNWAY_API_KEY and PIKA_API_KEY documentation"
      contains: "RUNWAY_API_KEY="
  key_links:
    - from: "src/services/video_engine/ai_generator.py"
      to: "src/api/config/settings.py"
      via: "AIVideoGeneratorService reads settings.RUNWAY_API_KEY / PIKA_API_KEY"
      pattern: "settings\\.(RUNWAY|PIKA)_API_KEY"
    - from: "src/api/routes/engines.py"
      to: "src/api/config/settings.py"
      via: "Availability endpoint reports key_set from settings"
      pattern: "settings\\.(RUNWAY|PIKA)_API_KEY"
---

<objective>
Activate the dormant Runway/Pika API integration in `AIVideoGeneratorService` by reading keys from `settings` (which already has the fields) instead of `os.getenv`. Add a `GET /engines/availability` endpoint that the dashboard can use to show/hide engines based on whether their keys are actually configured. Document the env vars in `.env.example`.
</objective>

<execution_context>
Single atomic commit. All edits are independent. Run validation in parallel.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/BACKLOG.md (item 999.3)
@src/services/video_engine/ai_generator.py
@src/api/config/settings.py
@src/api/routes/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch AIVideoGeneratorService to settings-based key resolution</name>
  <files>src/services/video_engine/ai_generator.py</files>
  <read_first>src/services/video_engine/ai_generator.py lines 1-60</read_first>
  <action>Replace `os.getenv` reads in `AIVideoGeneratorService.__init__` with settings-based reads. Specifically:
1. Add `from src.api.config import settings` at the top of the file (with the existing imports).
2. Change `self.provider = os.getenv("AI_VIDEO_PROVIDER", "none").lower()` to `self.provider = (settings.AI_VIDEO_PROVIDER or "none").lower()`.
3. Change `self.runway_key = os.getenv("RUNWAY_API_KEY", "")` to `self.runway_key = settings.RUNWAY_API_KEY or ""`.
4. Change `self.pika_key = os.getenv("PIKA_API_KEY", "")` to `self.pika_key = settings.PIKA_API_KEY or ""`.
5. Add a `# noqa: E402` if needed to keep the import order clean (the existing file uses `os`, `logging`, `asyncio` at top-level). The settings import is fine after them.
6. Keep the `import os` if it's still used elsewhere in the file (it isn't, so it's safe to remove; but leaving it is also fine).</action>
  <verify>
    <automated>cd /home/psalmprax/ALL_PROJECTS/ettametta && grep -n 'os.getenv\|settings\.' src/services/video_engine/ai_generator.py | head -10</automated>
  </verify>
  <done>No more os.getenv for AI_VIDEO_PROVIDER, RUNWAY_API_KEY, or PIKA_API_KEY in ai_generator.py; settings.X references are present</done>
</task>

<task type="auto">
  <name>Task 2: Create GET /engines/availability endpoint</name>
  <files>src/api/routes/engines.py</files>
  <read_first>src/api/routes/content_editor.py line 119 — existing engine list endpoint to match its style</read_first>
  <action>Create new file src/api/routes/engines.py with:
- APIRouter(prefix="/engines", tags=["Engines"])
- A Pydantic model `EngineAvailability` with: `id` (str), `name` (str), `provider` (str), `enabled` (bool), `key_set` (bool), `key_env_var` (str | None), `circuit_closed` (bool), `category` (str)
- GET /availability returning a list of EngineAvailability for: runway, pika, litx-video, ltx, hunyuan, mochi, cogvideo, wan, wan2.2, zeroscope, animatediff, lite4k, zsky, kling, pixverse, replicate, stability, haiper, luma
- The `key_set` and `key_env_var` come from the corresponding settings field; for the local GPU engines there is no key, so `key_set=True` (they work if the GPU is present) and `key_env_var=None`
- The `enabled` field reflects whether the engine is currently in `ENGINE_ACTION_MAP` AND (for keyed engines) whether the key is set
- The `circuit_closed` field is always True for now (we have a global circuit breaker but no per-engine state tracking yet; the field is wired so the dashboard can show it correctly when 12-02 adds it)
- Include a brief docstring explaining this endpoint is what the dashboard engine selector should call to know which engines to show</action>
  <verify>
    <automanded>cd /home/psalmprax/ALL_PROJECTS/ettametta && python -c "from src.api.routes.engines import router, EngineAvailability; print('OK:', len(router.routes), 'routes')"</automanded>
  </verify>
  <done>src/api/routes/engines.py exists, imports cleanly, has the GET /availability route</done>
</task>

<task type="auto">
  <name>Task 3: Register engines router in main.py</name>
  <files>src/api/main.py</files>
  <read_first>src/api/main.py — find where other routers (e.g. video, discovery) are included</read_first>
  <action>Add the engines router to src/api/main.py. The exact location depends on where other routers are included; typically `app.include_router(...)` calls are grouped together. Add `from src.api.routes.engines import router as engines_router` and `app.include_router(engines_router)` next to the other engine-related routers.</action>
  <verify>
    <automated>cd /home/psalmprax/ALL_PROJECTS/ettametta && grep -n 'engines_router\|from src.api.routes.engines' src/api/main.py</automated>
  </verify>
  <done>engines_router is imported and included in main.py</done>
</task>

<task type="auto">
  <name>Task 4: Document RUNWAY_API_KEY and PIKA_API_KEY in .env.example</name>
  <files>.env.example</files>
  <read_first>.env.example — find the ENABLE_PERSISTED_ANALYSIS section added in 10-01</read_first>
  <action>Append a new section to .env.example after the existing ENABLE_PERSISTED_ANALYSIS block. The section should be titled "Phase 12: AI Video Providers (Runway + Pika)" and include:
- `# AI_VIDEO_PROVIDER=none   # Set to "runway" or "pika" to activate; otherwise "none" disables the integration`
- `# RUNWAY_API_KEY=        # Get from https://app.runwayml.com → Settings → API Keys; 10 free credits on signup`
- `# PIKA_API_KEY=          # Get from https://pika.art → Settings → API Keys; 10 free credits/day`
- `# When AI_VIDEO_PROVIDER=runway + RUNWAY_API_KEY is set, the engine is active; same for pika.`
- `# The /engines/availability endpoint reports per-engine status for the dashboard.`</action>
  <verify>
    <automated>cd /home/psalmprax/ALL_PROJECTS/ettametta && tail -10 .env.example</automated>
  </verify>
  <done>RUNWAY_API_KEY and PIKA_API_KEY lines are present in .env.example with comments</done>
</task>

<task type="auto">
  <name>Task 5: Add unit tests for settings-based key resolution + /engines/availability</name>
  <files>tests/video_engine/test_ai_generator_keys.py, tests/api/test_engines_availability.py</files>
  <read_first>src/services/video_engine/ai_generator.py — the service under test</read_first>
  <action>Create tests/video_engine/test_ai_generator_keys.py with 3 tests:
1. test_ai_generator_reads_runway_key_from_settings — patch `src.services.video_engine.ai_generator.settings.RUNWAY_API_KEY` to "test-runway-key", instantiate AIVideoGeneratorService, assert service.runway_key == "test-runway-key"
2. test_ai_generator_reads_pika_key_from_settings — patch `src.services.video_engine.ai_generator.settings.PIKA_API_KEY` to "test-pika-key", instantiate, assert service.pika_key == "test-pika-key"
3. test_ai_generator_provider_uses_settings — patch settings.AI_VIDEO_PROVIDER to "runway" + settings.RUNWAY_API_KEY, assert service.enabled is True and service._get_api_key() == "test-runway-key"
Create tests/api/test_engines_availability.py with 2 tests:
1. test_engines_availability_includes_runway_and_pika — call the endpoint (with TestClient), assert both engines are in the response and have the expected shape
2. test_engines_availability_key_set_reflects_settings — patch settings.RUNWAY_API_KEY to a value, call the endpoint, assert runway.key_set is True
Use FastAPI TestClient and pytest.</action>
  <verify>
    <automated>cd /home/psalmprax/ALL_PROJECTS/ettametta && python -m pytest tests/video_engine/test_ai_generator_keys.py tests/api/test_engines_availability.py -v --tb=short 2>&1 | tail -15</automated>
  </verify>
  <done>All 5 tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings → Service | Settings values are operator-controlled; service must not crash on missing key |
| Endpoint → Browser | /engines/availability is read-only and reports boolean state — no secret leak risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Information Disclosure | /engines/availability | mitigate | Endpoint returns `key_set: bool`, never the key itself |
| T-12-01-02 | DoS | settings read on every request | accept | Settings is a singleton cached by pydantic; no perf concern |
| T-12-01-03 | Tampering | User overrides via UserSetting | accept | Settings reads from system config, not user overrides, for engine keys |

</threat_model>

<verification>
1. `python -c "from src.services.video_engine.ai_generator import AIVideoGeneratorService; ..."` — service still imports and reads from settings
2. `python -c "from src.api.routes.engines import router; ..."` — new router imports
3. `grep -n 'os.getenv' src/services/video_engine/ai_generator.py` — no os.getenv for the three engine keys
4. `python -m pytest tests/video_engine/test_ai_generator_keys.py tests/api/test_engines_availability.py -v` — 5 tests pass
5. `tail -10 .env.example` — RUNWAY_API_KEY and PIKA_API_KEY documented
</verification>

<success_criteria>
- AIVideoGeneratorService reads keys from settings
- GET /engines/availability returns per-engine status
- .env.example documents the new env vars
- 5 passing unit tests
- Existing tests not regressed
</success_criteria>

<output>
After completion, create .planning/phases/12-wire-runway-pika/12-01-SUMMARY.md
</output>
