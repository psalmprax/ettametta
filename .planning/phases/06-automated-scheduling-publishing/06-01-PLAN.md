---
phase: 06-automated-scheduling-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/services/optimization/scheduler.py", "src/api/utils/models.py"]
autonomous: true
requirements: ["PUBLISH-02"]
user_setup: []
must_haves:
  truths:
    - "User can schedule automated content publishing campaigns"
    - "SmartScheduler returns multiple optimal windows for multi-post days"
    - "ScheduledPostDB stores parallel flag and timezone info"
  artifacts:
    - path: "src/services/optimization/scheduler.py"
      provides: "SmartScheduler with multi-window support"
      contains: "calculate_n_optimal_windows"
    - path: "src/api/utils/models.py"
      provides: "Extended ScheduledPostDB"
      contains: "parallel_allowed|user_timezone"
  key_links:
    - from: "src/api/routes/publish.py"
      to: "src/services/optimization/scheduler.py"
      via: "calculate_n_optimal_windows"
      pattern: "calculate_n_optimal_windows"
---

<objective>
Extend SmartScheduler for multi-window scheduling and update ScheduledPostDB for smart parallel queuing
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-automated-scheduling-publishing/06-CONTEXT.md
@src/services/optimization/scheduler.py
@src/api/utils/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SmartScheduler for multi-window support</name>
  <files>src/services/optimization/scheduler.py</files>
  <action>Extend SmartScheduler class:
1. Add calculate_n_optimal_windows(n: int) method that returns top N optimal posting windows
2. Modify calculate_next_posting_time to accept optional 'count' parameter
3. Add parallel_allowed flag handling (4+ hours = parallel, else sequential)
4. Return engagement prediction percentages for each window</action>
  <verify>
    <automated>grep -c "calculate_n_optimal_windows\|parallel_allowed" src/services/optimization/scheduler.py | grep -q "2" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SmartScheduler extended with multi-window support</done>
</task>

<task type="auto">
  <name>Task 2: Extend ScheduledPostDB model</name>
  <files>src/api/utils/models.py</files>
  <action>Add new columns to ScheduledPostDB:
1. Add 'parallel_allowed' Boolean column (default False)
2. Add 'user_timezone' String column (nullable)
3. Add 'engagement_prediction' Float column (nullable)
4. Add 'optimal_rank' Integer column (nullable) - which optimal window (1st, 2nd, etc.)</action>
  <verify>
    <automated>grep -c "parallel_allowed\|user_timezone" src/api/utils/models.py | grep -q "2" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>ScheduledPostDB extended</done>
</task>

<task type="auto">
  <name>Task 3: Run database migration</name>
  <files></files>
  <action>Create and run Alembic migration for new ScheduledPostDB columns:
- parallel_allowed BOOLEAN DEFAULT FALSE
- user_timezone VARCHAR(50) NULL
- engagement_prediction FLOAT NULL
- optimal_rank INTEGER NULL</action>
  <verify>
    <automated>python -c "from src.api.utils.models import ScheduledPostDB; print(hasattr(ScheduledPostDB, 'parallel_allowed'))" 2>/dev/null && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Database migration complete</done>
</task>

</tasks>

<verification>
1. Verify SmartScheduler.calculate_n_optimal_windows(2) returns 2 windows
2. Verify ScheduledPostDB has all new columns
3. Verify API can read new fields from database
</verification>

<success_criteria>
- [ ] SmartScheduler supports multi-window scheduling
- [ ] ScheduledPostDB stores parallel flag and timezone
- [ ] Database schema updated
- [ ] API can return suggested times with predictions
</success_criteria>

---

*Phase: 06-automated-scheduling-publishing*
*Plan: 01*