---
phase: 06-automated-scheduling-publishing
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: ["src/api/routes/publish.py", "src/api/routes/schedule.py"]
autonomous: true
requirements: ["PUBLISH-02"]
user_setup: []
must_haves:
  truths:
    - "API returns AI-suggested optimal posting times"
    - "Schedule endpoint accepts and returns parallel flag"
    - "Scheduled posts include engagement prediction"
  artifacts:
    - path: "src/api/routes/publish.py"
      provides: "Schedule endpoints with suggestions"
      contains: "/schedule/suggested-times"
    - path: "src/api/routes/schedule.py"
      provides: "Schedule management"
      contains: "router"
  key_links:
    - from: "src/api/routes/publish.py"
      to: "src/services/optimization/scheduler.py"
      via: "calculate_n_optimal_windows"
      pattern: "calculate_n_optimal_windows"
---

<objective>
Add API endpoints for AI-suggested times and smart parallel scheduling
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-automated-scheduling-publishing/06-CONTEXT.md
@src/api/routes/publish.py
@src/services/optimization/scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add suggested-times endpoint</name>
  <files>src/api/routes/publish.py</files>
  <action>Add GET /schedule/suggested-times endpoint:
1. Accept query params: content_id (optional), count (default 3)
2. Call SmartScheduler.calculate_n_optimal_windows(count)
3. Return list of suggested times with:
   - scheduled_time (datetime)
   - engagement_prediction (float, percentage)
   - reason (string, e.g., "87% predicted engagement")
4. Include timezone conversion for display</action>
  <verify>
    <automated>grep -c "suggested-times\|calculate_n_optimal_windows" src/api/routes/publish.py | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Suggested times endpoint added</done>
</task>

<task type="auto">
  <name>Task 2: Update schedule endpoint for parallel</name>
  <files>src/api/routes/publish.py</files>
  <action>Update POST /schedule endpoint:
1. Accept new fields: parallel_allowed, user_timezone
2. Store new fields in ScheduledPostDB
3. Include engagement_prediction in response
4. Validate spacing if parallel_allowed is false (warn if <4 hours)</action>
  <verify>
    <automated>grep -c "parallel_allowed" src/api/routes/publish.py | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Schedule endpoint updated</done>
</task>

<task type="auto">
  <name>Task 3: Update scheduled endpoint response</name>
  <files>src/api/routes/publish.py</files>
  <action>Update GET /scheduled endpoint:
1. Include new fields in response: parallel_allowed, user_timezone, engagement_prediction, optimal_rank
2. Convert times to user's timezone if stored
3. Sort by scheduled_time</action>
  <verify>
    <automated>grep -c "engagement_prediction" src/api/routes/publish.py | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Scheduled endpoint updated</done>
</task>

</tasks>

<verification>
1. GET /schedule/suggested-times returns predictions
2. POST /schedule accepts parallel_allowed
3. GET /scheduled includes new fields
</verification>

<success_criteria>
- [ ] API returns suggested times with predictions
- [ ] Schedule accepts parallel flag
- [ ] Scheduled response includes new fields
</success_criteria>

---

*Phase: 06-automated-scheduling-publishing*
*Plan: 02*