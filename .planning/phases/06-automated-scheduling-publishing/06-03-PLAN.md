---
phase: 06-automated-scheduling-publishing
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: ["src/services/optimization/scheduler_tasks.py"]
autonomous: true
requirements: ["PUBLISH-02"]
user_setup: []
must_haves:
  truths:
    - "Missed schedules retry immediately on system recovery"
    - "Retry logic respects smart parallel spacing"
  artifacts:
    - path: "src/services/optimization/scheduler_tasks.py"
      provides: "Retry task for missed schedules"
      contains: "retry_missed_schedule"
  key_links:
    - from: "src/api/utils/celery.py"
      to: "src/services/optimization/scheduler_tasks.py"
      via: "retry_missed_schedule"
      pattern: "retry_missed_schedule"
---

<objective>
Implement retry logic for missed scheduled posts
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-automated-scheduling-publishing/06-CONTEXT.md
@src/services/optimization/scheduler_tasks.py
@src/api/utils/celery.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add retry task for missed schedules</name>
  <files>src/services/optimization/scheduler_tasks.py</files>
  <action>Create Celery task for retry logic:
1. Add @celery.task retry_missed_schedule()
2. Query ScheduledPostDB for posts where:
   - scheduled_time < now (missed)
   - status != "published"
   - retry_count < 3 (max retries)
3. For each missed post:
   - If parallel_allowed or >4 hours since last: post immediately
   - Else: reschedule to next optimal window
   - Increment retry_count
4. Run every 5 minutes via beat_schedule</action>
  <verify>
    <automated>grep -c "retry_missed_schedule" src/services/optimization/scheduler_tasks.py | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Retry task created</done>
</task>

<task type="auto">
  <name>Task 2: Update beat_schedule for retry</name>
  <files>src/api/utils/celery.py</files>
  <action>Add retry task to Celery beat_schedule:
- Task: "scheduler.retry_missed_schedule"
- Schedule: 300.0 (every 5 minutes)
- Add to scheduler_tasks.py imports in celery_app</action>
  <verify>
    <automated>grep -c "retry_missed_schedule\|300.0" src/api/utils/celery.py | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Beat schedule updated</done>
</task>

<task type="auto">
  <name>Task 3: Add retry_count to ScheduledPostDB</name>
  <files>src/api/utils/models.py</files>
  <action>Add retry tracking:
1. Add 'retry_count' Integer column (default 0)
2. Add 'last_retry_at' DateTime column (nullable)
3. Add 'status' values: pending, published, failed, retrying</action>
  <verify>
    <automated>grep -c "retry_count" src/api/utils/models.py | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Retry tracking added</done>
</task>

</tasks>

<verification>
1. retry_missed_schedule task runs every 5 minutes
2. Missed posts retry immediately on recovery
3. Retry count prevents infinite retries (max 3)
</verification>

<success_criteria>
- [ ] Missed schedules retry immediately
- [ ] Retry respects smart parallel spacing
- [ ] Max 3 retries prevents infinite loops
</success_criteria>

---

*Phase: 06-automated-scheduling-publishing*
*Plan: 03*