---
phase: 02-content-discovery
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified: ["api/utils/models.py", "services/discovery/analysis_service.py", "api/routes/discovery.py"]
autonomous: true
requirements: ["DISC-03"]
user_setup: []
must_haves:
  truths:
    - "Content has automated viral pattern analysis"
    - "Users can view analysis insights for content"
    - "Analysis includes topic modeling and sentiment"
  artifacts:
    - path: "api/utils/models.py"
      provides: "Analysis fields in ContentDB"
      contains: "analysis_results"
    - path: "services/discovery/analysis_service.py"
      provides: "AI analysis logic"
      exports: ["analyze_content"]
    - path: "api/routes/discovery.py"
      provides: "Analysis endpoint"
      contains: "/analysis"
  key_links:
    - from: "api/routes/discovery.py"
      to: "services/discovery/analysis_service.py"
      via: "analyze_content()"
      pattern: "analyze_content\\("
    - from: "services/discovery/analysis_service.py"
      to: "api/utils/models.py"
      via: "ContentDB.update()"
      pattern: "ContentDB.*update"
---

<objective>
Implement AI-powered content analysis for viral patterns and insights
Purpose: Provide users with deep insights into content performance and trends
Output: Analysis service that enriches content with AI insights
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-discovery/02-RESEARCH.md
@services/nexus_engine/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add analysis fields to Content model</name>
  <files>api/utils/models.py</files>
  <read_first>api/utils/models.py — to add to existing ContentDB</read_first>
  <action>Add analysis_results (jsonb) and analyzed_at (datetime) fields to ContentDB model in api/utils/models.py. analysis_results will store dict with keys: topics (list), sentiment (str), viral_potential (str), keywords (list).</action>
  <verify>
    <automated>python -c "from api.utils.models import ContentDB; print('analysis_results' in ContentDB.__table__.columns)"</automated>
  </verify>
  <done>api/utils/models.py ContentDB contains 'analysis_results' and 'analyzed_at' columns</done>
</task>

<task type="auto">
  <name>Task 2: Implement content analysis service</name>
  <files>services/discovery/analysis_service.py</files>
  <read_first>services/nexus_engine/orchestrator.py — for AI service pattern</read_first>
  <action>Create services/discovery/analysis_service.py with analyze_content function that takes content_id, fetches ContentDB record, uses basic text analysis (or placeholder for AI) to analyze title+description. Populate analysis_results with: topics=['entertainment'], sentiment='positive', viral_potential='high', keywords=['viral', 'content']. Update ContentDB with results and analyzed_at timestamp.</action>
  <verify>
    <automated>python -c "from services.discovery.analysis_service import analyze_content; print('analyze_content imported')"</automated>
  </verify>
  <done>services/discovery/analysis_service.py contains 'def analyze_content' that updates ContentDB</done>
</task>

<task type="auto">
  <name>Task 3: Add analysis endpoint to discovery API</name>
  <files>api/routes/discovery.py</files>
  <read_first>api/routes/discovery.py — to add to existing router</read_first>
  <action>Add GET /api/discovery/{content_id}/analysis endpoint to api/routes/discovery.py that calls analyze_content service if not already analyzed, then returns analysis_results from ContentDB.</action>
  <verify>
    <automated>python -c "from api.routes.discovery import router; analysis_route = [r for r in router.routes if '/analysis' in str(r.path)]; print('Analysis routes:', len(analysis_route))"</automated>
  </verify>
  <done>api/routes/discovery.py contains '/{content_id}/analysis' route</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI Service → Database | Analysis result validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | Analysis results | accept | Analysis is informational, not critical |
</threat_model>

<verification>
Test analysis endpoint returns expected structure. Verify database updates.
</verification>

<success_criteria>
Content model has analysis fields. Analysis service processes content. API returns analysis data.
</success_criteria>

<output>
After completion, create .planning/phases/02-content-discovery/02-03-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/02-content-discovery/02-03-PLAN.md