---
phase: 02-content-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["api/utils/models.py", "services/discovery/scanner_service.py", "services/discovery/youtube_scanner.py", "api/utils/scheduler.py"]
autonomous: true
requirements: ["DISC-01"]
user_setup: [{"service": "google-apis", "why": "YouTube Data API access", "env_vars": [{"name": "YOUTUBE_API_KEY", "source": "Google Cloud Console -> APIs & Services -> Credentials"}], "dashboard_config": []}]
must_haves:
  truths:
    - "Automated scanners collect trending content from YouTube"
    - "Content data is stored in the database with complete metadata"
    - "Scanners run periodically without manual intervention"
  artifacts:
    - path: "api/utils/models.py"
      provides: "Content database model"
      contains: "class ContentDB"
    - path: "services/discovery/scanner_service.py"
      provides: "Scanner orchestration"
      exports: ["scan_trending_content"]
    - path: "services/discovery/youtube_scanner.py"
      provides: "YouTube API integration"
      exports: ["YouTubeScanner"]
    - path: "api/utils/scheduler.py"
      provides: "Periodic task scheduling"
      contains: "celery_app.conf.beat_schedule"
  key_links:
    - from: "services/discovery/scanner_service.py"
      to: "api/utils/models.py"
      via: "ContentDB.create()"
      pattern: "ContentDB.*create"
    - from: "api/utils/scheduler.py"
      to: "services/discovery/scanner_service.py"
      via: "scan_trending_content.delay()"
      pattern: "scan_trending_content\\.delay"
---

<objective>
Implement automated trending content collection from YouTube
Purpose: Enable users to view fresh trending content as the foundation for discovery
Output: Working scanner system that populates the content database
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-discovery/02-RESEARCH.md
@api/utils/models.py
@services/discovery/scanner_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Content database model</name>
  <files>api/utils/models.py</files>
  <read_first>api/utils/models.py — to see existing UserDB model pattern</read_first>
  <action>Add ContentDB model to api/utils/models.py with fields: id (UUID), platform (str), external_id (str, unique), title (str), description (text), creator_name (str), creator_id (str), url (str), thumbnail_url (str), duration_seconds (int), view_count (bigint), like_count (bigint), comment_count (bigint), share_count (bigint), published_at (datetime), scanned_at (datetime), viral_score (decimal), tags (array str), metadata (jsonb). Use SQLAlchemy declarative base like UserDB.</action>
  <verify>
    <automated>python -c "from api.utils.models import ContentDB; print('ContentDB imported successfully')"</automated>
  </verify>
  <done>api/utils/models.py contains 'class ContentDB(Base):' and all specified fields</done>
</task>

<task type="auto">
  <name>Task 2: Implement YouTube scanner service</name>
  <files>services/discovery/youtube_scanner.py, services/discovery/scanner_service.py</files>
  <read_first>services/video_engine/synthesis_service.py — to see existing service pattern</read_first>
  <action>Create services/discovery/youtube_scanner.py with YouTubeScanner class that uses google-api-python-client to fetch trending videos via videos().list(chart='mostPopular'). Extract fields: id, snippet(title, description, channelTitle, channelId, publishedAt, thumbnails), statistics(viewCount, likeCount, commentCount). Create services/discovery/scanner_service.py with scan_trending_content function that calls YouTubeScanner and saves to ContentDB, handling duplicates by external_id.</action>
  <verify>
    <automated>python -c "from services.discovery.youtube_scanner import YouTubeScanner; scanner = YouTubeScanner(); print('YouTubeScanner instantiated')"</automated>
  </verify>
  <done>services/discovery/youtube_scanner.py contains 'class YouTubeScanner' and services/discovery/scanner_service.py contains 'def scan_trending_content'</done>
</task>

<task type="auto">
  <name>Task 3: Add periodic scanner scheduling</name>
  <files>api/utils/scheduler.py</files>
  <read_first>api/utils/scheduler.py — if exists, else check existing Celery config</read_first>
  <action>Create or update api/utils/scheduler.py to add Celery beat schedule for 'scan-trending' task that runs services.discovery.scanner_service.scan_trending_content every 2 hours. If scheduler.py doesn't exist, create it with celery_app.conf.beat_schedule = {'scan-trending': {'task': 'services.discovery.scanner_service.scan_trending_content', 'schedule': 7200.0}}</action>
  <verify>
    <automated>python -c "from api.utils.scheduler import celery_app; print('beat_schedule keys:', list(celery_app.conf.beat_schedule.keys()))"</automated>
  </verify>
  <done>api/utils/scheduler.py contains 'scan-trending' in beat_schedule with 7200.0 schedule</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External API → Scanner | Untrusted YouTube API responses |
| Scanner → Database | Content data validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | YouTube API key | mitigate | Store YOUTUBE_API_KEY in environment variables, never in code |
| T-02-02 | Tampering | Content data | mitigate | Validate API response structure before saving to database |
| T-02-03 | Denial of Service | Scanner overload | accept | Rate limiting via Celery beat schedule prevents abuse |
</threat_model>

<verification>
Run database migration to create content table. Test scanner manually with YOUTUBE_API_KEY set.
</verification>

<success_criteria>
ContentDB model exists with all fields. YouTubeScanner can fetch trending videos. Scheduler configured for periodic runs.
</success_criteria>

<output>
After completion, create .planning/phases/02-content-discovery/02-01-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/02-content-discovery/02-01-PLAN.md