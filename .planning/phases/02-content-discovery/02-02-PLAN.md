---
phase: 02-content-discovery
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: ["api/routes/discovery.py", "services/discovery/search_service.py"]
autonomous: true
requirements: ["DISC-02"]
user_setup: []
must_haves:
  truths:
    - "Users can search content with platform filters"
    - "Content can be sorted by viral score"
    - "Search results include all relevant metadata"
  artifacts:
    - path: "api/routes/discovery.py"
      provides: "Discovery API endpoints"
      exports: ["router"]
    - path: "services/discovery/search_service.py"
      provides: "Search logic and filtering"
      exports: ["search_content"]
  key_links:
    - from: "api/routes/discovery.py"
      to: "services/discovery/search_service.py"
      via: "search_content()"
      pattern: "search_content\\("
    - from: "services/discovery/search_service.py"
      to: "api/utils/models.py"
      via: "ContentDB.query"
      pattern: "ContentDB\\.query"
---

<objective>
Implement content search API with filters and viral score sorting
Purpose: Enable users to find specific content using advanced search capabilities
Output: Working search endpoint with filtering and sorting
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-discovery/02-RESEARCH.md
@api/routes/auth.py
@api/routes/discovery.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create search service with filtering</name>
  <files>services/discovery/search_service.py</files>
  <read_first>services/video_engine/synthesis_service.py — for service pattern</read_first>
  <action>Create services/discovery/search_service.py with search_content function that accepts filters: platform (list), min_views (int), min_viral_score (float), creator (str), tags (list), date_from (datetime), date_to (datetime). Use SQLAlchemy query on ContentDB with filters. Support sorting by viral_score DESC, published_at DESC, view_count DESC. Return paginated results with limit/offset.</action>
  <verify>
    <automated>python -c "from services.discovery.search_service import search_content; print('search_content imported')"</automated>
  </verify>
  <done>services/discovery/search_service.py contains 'def search_content' with all filter parameters</done>
</task>

<task type="auto">
  <name>Task 2: Implement discovery API routes</name>
  <files>api/routes/discovery.py</files>
  <read_first>api/routes/auth.py — for FastAPI router pattern and Pydantic models</read_first>
  <action>Create api/routes/discovery.py with FastAPI router. Add GET /api/discovery/search endpoint with query parameters: platform, min_views, min_viral_score, creator, tags, date_from, date_to, sort_by, limit, offset. Use Pydantic models for request/response. Call search_content service and return results. Add GET /api/discovery/trending endpoint that returns top 50 by viral_score.</action>
  <verify>
    <automated>python -c "from api.routes.discovery import router; print('router has', len(router.routes), 'routes')"</automated>
  </verify>
  <done>api/routes/discovery.py contains router with '/search' and '/trending' routes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Service | User input validation for search parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Injection | Search parameters | mitigate | Use parameterized SQLAlchemy queries, validate input types |
| T-02-05 | Information Disclosure | Content data exposure | accept | Public content discovery is intentional |
</threat_model>

<verification>
Test search endpoint with various filters. Verify SQL injection protection.
</verification>

<success_criteria>
Search service filters content correctly. API endpoints return expected results.
</success_criteria>

<output>
After completion, create .planning/phases/02-content-discovery/02-02-SUMMARY.md
</output></content>
<parameter name="filePath">.planning/phases/02-content-discovery/02-02-PLAN.md