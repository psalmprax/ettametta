---
phase: 17-revenue-txid-backfill-idempotency
plan: 01
type: execute
wave: 1
depends_on: [] # No code dependency; promotion + verification only
files_modified: [
  ".planning/ROADMAP.md",
  ".planning/BACKLOG.md",
  ".planning/STATE.md",
  ".planning/phases/17-revenue-txid-backfill-idempotency/17-01-SUMMARY.md",
  "tests/migrations/test_revenue_txid_migrations.py"
]
autonomous: true
requirements: ["MONET-01"]
user_setup: ["DATABASE_URL with write access to a disposable schema for running the integration test"]
must_haves:
  truths:
    - "alembic/versions/2026_06_16_backfill_txid.py's _BACKFILL_SQL candidates CTE excludes groups that already have a non-NULL transaction_id (NOT IN subquery on (platform, transaction_id))"
    - "tests/migrations/test_revenue_txid_migrations.py has NO @pytest.mark.xfail decorator on the re-run idempotency test"
    - "Running the backfill SQL three consecutive times against a fresh schema produces exactly two rows updated on pass-1 and zero rows updated on pass-2 and pass-3, with zero UniqueViolation errors"
    - "After all three passes, row a-002 (the documented duplicate loser) remains transaction_id=NULL"
    - ".planning/ROADMAP.md enumerates Phase 17 as Not Started pointing to this plan; .planning/BACKLOG.md marks 999.5 as PROMOTED → Phase 17"
  artifacts:
    - path: "alembic/versions/2026_06_16_backfill_txid.py"
      provides: "Backfill migration with idempotent candidates CTE"
      contains: "(platform, metadata_json->>'transaction_id') NOT IN"
    - path: "tests/migrations/test_revenue_txid_migrations.py"
      provides: "TestBackfillFixturesOnPostgres with re-run + 3-pass coverage"
      contains: "def test_backfill_is_idempotent_on_rerun"
    - path: ".planning/phases/17-revenue-txid-backfill-idempotency/17-01-PLAN.md"
      provides: "This plan"
      contains: "phase: 17-revenue-txid-backfill-idempotency"
  key_links:
    - from: "alembic/versions/2026_06_16_backfill_txid.py"
      to: "tests/migrations/test_revenue_txid_migrations.py"
      via: "_BACKFILL_SQL constant is parsed via _module_const() and executed against a Postgres fixture schema"
      pattern: "_BACKFILL_SQL = sa.text"
    - from: ".planning/BACKLOG.md"
      to: ".planning/ROADMAP.md"
      via: "Item 999.5 status flips to PROMOTED → Phase 17; .planning/ROADMAP.md adds a Phase 17 row referencing this plan"
      pattern: "PROMOTED → Phase 17"
---

<objective>
Promote BACKLOG 999.5 (revenue_txid backfill re-run idempotency) to a numbered GSD phase, verify the SQL fix in `alembic/versions/2026_06_16_backfill_txid.py` is structurally correct (it is), remove any lingering `@pytest.mark.xfail` markers (already removed; verified by grep), and add an explicit 3-pass integration test that proves the backfill is truly idempotent against a real Postgres. Doc changes: BACKLOG 999.5 → "PROMOTED → Phase 17", ROADMAP gains a Phase 17 entry, STATE.md gains a 17-01 pending todo.

**Prod-data caveat:** The backfill migration is already deployed to production (BACKLOG 999.5 logs the prod application as 2026-06-15). After deployment, prod `revenue_logs` rows that had a `metadata_json->>'transaction_id'` value already had their top-level `transaction_id` column populated. The candidates CTE (with the `NOT IN` exclusion) correctly sees these as already-backfilled and skips them on any re-run. **Any DB-backed test against a prod-shaped fixture therefore expects 0 rows updated** — that is the correct no-op behavior, not a failure. Tests on a fresh schema (the 7-fixture test) should still expect 2 writers + 5 untouched + 0 violators.

**Important context (already verified prior to drafting):**

- The `NOT IN` subquery fix that BACKLOG 999.5 prescribes is already in `alembic/versions/2026_06_16_backfill_txid.py` (the candidates CTE has `(platform, metadata_json->>'transaction_id') NOT IN (SELECT platform, transaction_id FROM revenue_logs WHERE transaction_id IS NOT NULL)`). The migration's docstring explicitly claims "Re-run is a true no-op (0 rows updated). Safe to re-run."
- The DB-backed idempotency test `TestBackfillFixturesOnPostgres::test_backfill_is_idempotent_on_rerun` already asserts `first == 2, second == 0`.
- A `grep -n 'xfail' tests/migrations/test_revenue_txid_migrations.py` returns zero matches; no `strict=True` marker remains to break CI.

So 80%+ of the work is already in place. This plan covers the remaining intent: (a) make the idempotency contract structurally bullet-proof (3 passes, structural assertions, NOT IN subquery explicitly tested), (b) reflect the promotion in tracking docs.
</objective>

<execution_context>
Promotion + verification plan. The SQL fix and xfail removal are already in place; this plan adds an explicit 3-pass integration test and updates tracking docs. No alembic revision change is required.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/BACKLOG.md (item 999.5)
@.planning/STATE.md
@alembic/versions/2026_06_16_backfill_txid.py (the migration — DO NOT EDIT, it already has the fix)
@tests/migrations/test_revenue_txid_migrations.py (the test file — extend with 3-pass test)
@alembic/versions/merge_remaining_2026.py (down_revision chain context)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the NOT IN subquery is structurally in the migration</name>
  <files>alembic/versions/2026_06_16_backfill_txid.py</files>
  <read_first>alembic/versions/2026_06_16_backfill_txid.py lines 50-100 (the _BACKFILL_SQL constant)</read_first>
  <action>Run a static structural assertion against the existing migration to lock the contract in CI. Do NOT edit the migration. Add the assertion as a Python test (in tests/migrations/test_revenue_txid_migrations.py, in the existing TestBackfillStructure class) that:
1. Parses the migration with `ast.parse`.
2. Reads the `_BACKFILL_SQL` constant via the existing `_module_const` helper.
3. Asserts `'(platform, metadata_json->>\\'transaction_id\\') NOT IN'` is present.
4. Asserts `'SELECT platform, transaction_id\\n              FROM revenue_logs\\n              WHERE transaction_id IS NOT NULL'` is present (the inner subquery body).
5. Asserts the WHERE clause above the NOT IN still requires `transaction_id IS NULL` (so the inner subquery is what makes re-runs idempotent, not the outer NULL filter).
This is a regression-catcher: a future "cleanup" of the migration that drops the NOT IN will fail this test loudly.</action>
  <verify>
    <automated>python -m pytest tests/migrations/test_revenue_txid_migrations.py::TestBackfillStructure -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>The migration's `_BACKFILL_SQL` AST parses, the NOT IN subquery is present, and a regression that drops it fails this test.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit 3-pass idempotency integration test</name>
  <files>tests/migrations/test_revenue_txid_migrations.py</files>
  <read_first>tests/migrations/test_revenue_txid_migrations.py — TestBackfillFixturesOnPostgres class around the existing `test_backfill_is_idempotent_on_rerun` test (uses `_run_backfill`, `_setup_revenue_logs`, `_insert_fixtures`, `_query`)</read_first>
  <action>Inside the existing `TestBackfillFixturesOnPostgres` class (a Postgres-gated class that auto-skips when DATABASE_URL is unset), add ONE new test method:

```python
def test_backfill_is_idempotent_over_three_passes(self, pg_db_url, pg_schema):
    """Run the backfill three consecutive times. Pass-1 writes the
    canonical 2 winners (a-001 + b-001). Pass-2 and pass-3 must update
    exactly 0 rows (true no-op), and no UniqueViolation must be raised
    at any point (psycopg2.IntegrityError would abort the test).
    """
    _setup_revenue_logs(pg_db_url, pg_schema)
    _run_migration_sql(pg_db_url, pg_schema, MIGRATION_COLUMN_ADD, "upgrade")
    _insert_fixtures(pg_db_url, pg_schema)

    first = _run_backfill(pg_db_url, pg_schema)
    second = _run_backfill(pg_db_url, pg_schema)
    third = _run_backfill(pg_db_url, pg_schema)

    assert first == 2, f"pass-1 should write 2 winners, got {first}"
    assert second == 0, f"pass-2 should be a no-op (0 rows), got {second}"
    assert third == 0, f"pass-3 should be a no-op (0 rows), got {third}"

    # Loser row stays NULL (not retroactively overwritten).
    rows = _query(
        pg_db_url, pg_schema,
        "SELECT id, transaction_id FROM revenue_logs WHERE id = 'a-002'",
    )
    assert rows == [("a-002", None)], rows
```

Verify it runs cleanly when DATABASE_URL is set; verify it cleanly skips when DATABASE_URL is unset.</action>
  <verify>
    <automated>DATABASE_URL=postgresql://test:test@localhost:5432/test python -m pytest tests/migrations/test_revenue_txid_migrations.py::TestBackfillFixturesOnPostgres::test_backfill_is_idempotent_over_three_passes -v --tb=short 2>&1 | tail -20; echo '---SKIP CASE---'; unset DATABASE_URL; python -m pytest tests/migrations/test_revenue_txid_migrations.py::TestBackfillFixturesOnPostgres::test_backfill_is_idempotent_over_three_passes -v --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>3-pass test added to the DB-backed class. With DATABASE_URL unset, pytest reports SKIPPED (auto-skip preserved). With DATABASE_URL set, the test passes if and only if the migration's NOT IN subquery is intact.</done>
</task>

<task type="auto">
  <name>Task 3: Run full migration test module locally to verify nothing else regressed</name>
  <files>tests/migrations/test_revenue_txid_migrations.py</files>
  <read_first>tests/migrations/test_revenue_txid_migrations.py — top of file</read_first>
  <action>The file has an outer `pytestmark` and three classes: `TestUpgradeChainWellFormed`, `TestColumnAddIdempotency`, `TestBackfillStructure`, `TestBackfillFixturesOnPostgres`. The first three are pure-AST tests (no DB). The fourth is DB-gated. After Task 1 and Task 2 land, run the entire module locally:

```bash
cd /home/psalmprax/ALL_PROJECTS/ettametta && python -m pytest tests/migrations/test_revenue_txid_migrations.py -v --tb=short
```

Expected outcome: every pure-AST test PASSES; the DB-backed `TestBackfillFixturesOnPostgres` class either passes (if DATABASE_URL set) or cleanly SKIPS (if unset). No test should be xfail (grep should still return zero matches for `xfail` after the task is done).</action>
  <verify>
    <automated>python -m pytest tests/migrations/test_revenue_txid_migrations.py -v --tb=short 2>&1 | tail -30; echo '---NO-XFAIL---'; grep -rn 'xfail' tests/migrations/test_revenue_txid_migrations.py || echo 'NO_XFAIL_FOUND'</automated>
  </verify>
  <done>All pure-AST tests pass; DB-backed tests pass-or-skip; no `xfail` markers present.</done>
</task>

<task type="auto">
  <name>Task 4: Update .planning/ROADMAP.md to enumerate Phase 17</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>.planning/ROADMAP.md — top phases list (lines 5-30) and Phase Details (Phase 10/11/12/13)</read_first>
  <action>Add Phase 17 entry to:
- The main `## Phases` bullet list (~ line 30, after the Phase 13 entry, before "## Phase Details")
- The summary table at the bottom (~line 220, after Phase 13's row)
- A `### Phase 17` section under `## Phase Details` (~ after Phase 13's section, following the existing format: Goal / Depends on / Requirements / Success Criteria / Plans)

Phase 17 text to use:

```
- [ ] **Phase 17: Revenue transaction_id Backfill Idempotency** - Verify and lock the canonical idempotent backfill of revenue_logs.transaction_id from metadata_json so re-running the migration never raises UniqueViolation
    - Promoted from `.planning/BACKLOG.md` item 999.5 (P0)
    - [ ] 17-01-PLAN.md — Verify NOT IN subquery is in _BACKFILL_SQL + add 3-pass integration test + remove xfail (if reintroduced)
```

Success Criteria (5 bullets, mirroring BACKLOG 999.5 acceptance):
1. `alembic/versions/2026_06_16_backfill_txid.py` `_BACKFILL_SQL` candidates CTE excludes groups that already have a non-NULL transaction_id (NOT IN subquery on `(platform, transaction_id)`)
2. `tests/migrations/test_revenue_txid_migrations.py` has no `@pytest.mark.xfail` decorators on `test_backfill_is_idempotent_on_rerun` (CI must not depend on `strict=True` semantics)
3. Running the backfill SQL once against the prod-shaped schema writes the canonical 2 winners (a-001 oldest duplicate + b-001 normal); a-002 (the documented loser) stays NULL
4. Running the backfill SQL a second and third time against the same schema updates exactly 0 rows with zero UniqueViolation errors
5. `grep -rn 'xfail' tests/migrations/ alembic/versions/` returns no markers in the revenue_txid path

Do NOT add Phases 14, 15, 16 to the roadmap in this plan — those are "ghost phases" with on-disk artifacts but no prior roadmap recording; syncing them is a SEPARATE concern (recommend a `gsd-prune-phases` or retroactive sweep).</action>
  <verify>
    <automated>grep -n 'Phase 17\|17-01' .planning/ROADMAP.md && echo '---SUCCESS---' || echo 'PHASE 17 NOT FOUND'</automated>
  </verify>
  <done>.planning/ROADMAP.md enumerates Phase 17 with this plan as the only plan; ROADMAP does not change for phases 14/15/16 (separate concern).</done>
</task>

<task type="auto">
  <name>Task 5: Update .planning/BACKLOG.md to mark 999.5 as PROMOTED → Phase 17</name>
  <files>.planning/BACKLOG.md</files>
  <read_first>.planning/BACKLOG.md — search for "### 999.5" (the section heading for the revenue_txid item)</read_first>
  <action>Change the section heading from:

```
### 999.5 — Fix revenue_txid Backfill Re-Run Idempotency
```

to:

```
### ~~999.5 — Fix revenue_txid Backfill Re-Run Idempotency~~ ⮕ **PROMOTED → Phase 17 (2026-06-19)**
```

Also append the standard "Status:" footer block (matching the format used for 999.1/999.2/999.3/999.4):

```
**Status:** Promoted to Phase 17 in ROADMAP.md. Execution plan: `.planning/phases/17-revenue-txid-backfill-idempotency/17-01-PLAN.md`.
```

Do NOT touch 999.6/999.7/999.8 in this plan — those correspond to ghost phases 14/15/16 which are out of scope for promotion here.</action>
  <verify>
    <automated>grep -n '999.5' .planning/BACKLOG.md && echo '---STRIKETHROUGH VERIFIED---' || echo '999.5 NOT MARKED'</automated>
  </verify>
  <done>BACKLOG.md item 999.5 is struck through and labeled "PROMOTED → Phase 17"; a Status footer points readers to the new plan file.</done>
</task>

<task type="auto">
  <name>Task 6: Update .planning/STATE.md to reflect Phase 17 promotion</name>
  <files>.planning/STATE.md</files>
  <read_first>.planning/STATE.md frontmatter + "Pending Todos" + "Session Continuity"</read_first>
  <action>Make three minimal edits:

1. Frontmatter `progress.total_phases: 10` → `14` (phases 1–13 + the new phase 17; ghost phases 14/15/16 not counted here pending separate sync).
2. Add to `Pending Todos`:
   ```
   - Phase 17 Plan 01: Verify _BACKFILL_SQL NOT IN subquery, add 3-pass idempotency integration test
     - Tests/migration files: `tests/migrations/test_revenue_txid_migrations.py` (no migration edit needed)
     - Sub-plans: single plan (no 17-02..17-N expected unless DB test fails)
   ```
3. Update `stopped_at:` line and `Session Continuity` `Resume context:` to mention Phase 17 instead of Phase 10 ("Promoting BACKLOG 999.5 → Phase 17 (Backfill Idempotency) and beginning execution").

Do NOT bump `completed_phases` (still 6 — none of the ghost phases 14/15/16 are being marked complete here) or `completed_plans` (16 — no plan completed in this draft).</action>
  <verify>
    <automated>head -15 .planning/STATE.md && echo '---PENDING---' && grep -A2 -B2 'Phase 17' .planning/STATE.md</automated>
  </verify>
  <done>STATE.md frontmatter counts Phase 17 in; Pending Todos lists 17-01; Session Continuity points to Phase 17.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production Postgres → Backfill migration | Migration runs against revenue_logs on prod. The candidates CTE must not raise UniqueViolation on re-run. |
| Test harness → Disposable schema | The 3-pass test creates + drops a temp schema; cannot accidentally affect any other database state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Tampering | `_BACKFILL_SQL` | mitigate | Task 1 AST-based regression test asserts NOT IN subquery is present |
| T-17-01-02 | Repudiation | Re-run idempotency | mitigate | Task 2 3-pass integration test asserts zero rows + zero UniqueViolation on passes 2 and 3 |
| T-17-01-03 | Information Disclosure | Backfill rowcount | accept | Print is for operator visibility only; no PII in metadata_json expected |
| T-17-01-04 | DoS | Repeated re-runs | mitigate | Idempotent CTE makes re-runs a true no-op (0 rows updated; test enforces this) |

</threat_model>

<verification>
1. `python -m pytest tests/migrations/test_revenue_txid_migrations.py::TestBackfillStructure -v` — AST tests pass
2. `python -m pytest tests/migrations/test_revenue_txid_migrations.py::TestBackfillFixturesOnPostgres::test_backfill_is_idempotent_over_three_passes -v` — 3-pass test passes (or SKIPs if DATABASE_URL unset)
3. `grep -rn 'xfail' tests/migrations/ alembic/versions/2026_06_16_backfill_txid.py` returns zero matches (xfail removed)
4. `.planning/ROADMAP.md` contains a Phase 17 entry pointing at `17-01-PLAN.md`
5. `.planning/BACKLOG.md` item 999.5 is strikethrough-struck and labeled "PROMOTED → Phase 17"
6. `.planning/STATE.md` frontmatter `total_phases: 14` and "Pending Todos" includes Phase 17-01
</verification>

<success_criteria>
- Phase 17 promotion recorded in BACKLOG.md, ROADMAP.md, STATE.md
- `tests/migrations/test_revenue_txid_migrations.py` extends `TestBackfillFixturesOnPostgres` with `test_backfill_is_idempotent_over_three_passes` (or an equivalent) that proves the backfill is a true no-op on passes 2-3
- `TestBackfillStructure` enforces the NOT IN subquery is in `_BACKFILL_SQL` (regression catch for future cleanups)
- No `@pytest.mark.xfail` markers anywhere in the revenue_txid migration/test path
- All existing migration tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/17-revenue-txid-backfill-idempotency/17-01-SUMMARY.md` mirroring the format of `.planning/phases/14-affiliate-auto-insert/14-01-SUMMARY.md`. The summary should explicitly note that 80% of the work was already in place (SQL fix + xfail removal) and that this plan was promotion + verification only.
</output>
